text animation
Wave Color Shift
gsap.registerPlugin(SplitText);
const split = SplitText.create(".text", { type: "chars" });

gsap.from(split.chars, {
  y: 40, color: "#00FF66", opacity: 0,
  stagger: { each: 0.04, from: "start" },
  duration: 0.6, ease: "sine.out"
});


Ink Bleed
gsap.registerPlugin(SplitText);
const split = SplitText.create(".text", { type: "chars" });

gsap.from(split.chars, {
  y: 40, color: "#00FF66", opacity: 0,
  stagger: { each: 0.04, from: "start" },
  duration: 0.6, ease: "sine.out"
});



Binary Decode
gsap.registerPlugin(TextPlugin);

gsap.to(".text", {
  text: { value: "DECODED", chars: "01", speed: 0.3 },
  duration: 2.5, ease: "none"
});

Neon Flicker
const tl = gsap.timeline();
tl.set(".text", { opacity: 0 })
  .to(".text", { opacity: 1, duration: 0.1 })
  .to(".text", { opacity: 0.2, duration: 0.05 })
  .to(".text", { opacity: 1, duration: 0.1 })
  .to(".text", { opacity: 0, duration: 0.05 })
  .to(".text", { opacity: 1, textShadow: "0 0 20px #00FF66", duration: 0.3 });



Typewriter Effect
gsap.registerPlugin(TextPlugin);

gsap.to(".text", {
  text: "Hello, World!",
  duration: 2, ease: "none"
});






gsapify

menu hover 
Gooey Menu Hover
Nav links have a blob that stretches between items with a viscous feel.

MorphSVG
function moveBlob(target) {
  const rect = target.getBoundingClientRect();
  gsap.to(".blob", {
    left: rect.left - nav.left,
    width: rect.width + 16,
    duration: 0.4,
    ease: "power2.out"
  });
}


text option 
Typewriter
Text types out character by character with a blinking cursor.

TextPlugin
gsap.to(".target", {
  text: { value: "Hello, World!" },
  duration: 2,
  ease: "none"
});


background animation 

Particle Float Field
Subtle floating particles drift upward with randomized paths.

Core GSAP
gsap.to(particle, {
  y: gsap.utils.random(-150, -50),
  x: gsap.utils.random(-30, 30),
  opacity: 0,
  duration: gsap.utils.random(3, 6),
  repeat: -1
});


Aurora Borealis Waves
Soft layered gradient waves undulate across the background like northern lights.

Core GSAP
const tl = gsap.timeline({ repeat: -1, yoyo: true });
waves.forEach((wave, i) => {
  tl.to(wave, {
    y: -20 + i * 10,
    scaleX: 1.2,
    opacity: 0.4,
    duration: 3 + i,
    ease: "sine.inOut"
  }, 0);
});




freefrontend website for clonable frontend elements






great now as i am in second page i need to keep that part still and show all the details like about us... work collabration brands... and then review of people and then some images with that we can show the footer 






















